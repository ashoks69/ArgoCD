apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Ashok
  template:
    metadata:
      labels:
        app: Ashok
    spec:
      containers:
        - name: pavan
          image: shaikmustafa/cycle:latest
          ports:
            - containerPort: 80
